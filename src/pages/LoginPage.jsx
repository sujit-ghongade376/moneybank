import React from "react";
import LoginForm from "../components/LoginForm";

const LoginPage = () => (
  <div style={{ background: "#f5f6fa", minHeight: "100vh", minWidth: "100vw" }}>
    <LoginForm />
  </div>
);

export default LoginPage;
